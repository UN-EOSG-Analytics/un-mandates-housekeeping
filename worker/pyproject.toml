[project]
name = "un80-docx-worker"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "azure-storage-blob",
    "psycopg[binary]",
    "python-docx",
    # add your existing pipeline deps here
]

[project.scripts]
un80-docx-worker = "un80-docx-worker.main:cli"
