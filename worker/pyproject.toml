[project]
name = "ppb-docx-worker"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "azure-storage-blob",
    "psycopg[binary]",
    "python-docx",
    # add your existing pipeline deps here
]

[project.scripts]
ppb-docx-worker = "ppb-docx-worker.main:cli"
